function DurationExercise({ name }) {
  return (
    <div>
      <p style={{ color: '#888' }}>Type: Duration</p>
      <p>{name} — DurationExercise placeholder</p>
    </div>
  );
}

export default DurationExercise;
